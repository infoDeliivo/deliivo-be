{
  "paths": {
    "/api/v1/driver/bookings/{id}/accept": {
      "post": {
        "operationId": "driverBookingsPostByIdAccept",
        "summary": "POST driver-bookings/:id/accept",
        "tags": [
          "Driver Bookings"
        ],
        "security": [
          {
            "BearerAuth": []
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "../components/schemas/common.yaml#/ApiSuccessEnvelope"
                },
                "examples": {
                  "default": {
                    "$ref": "../components/examples/common.yaml#/DriverBookingSuccess"
                  }
                }
              }
            }
          },
          "400": {
            "$ref": "../components/responses/errors.yaml#/BadRequest"
          },
          "401": {
            "$ref": "../components/responses/errors.yaml#/Unauthorized"
          },
          "403": {
            "$ref": "../components/responses/errors.yaml#/Forbidden"
          },
          "404": {
            "$ref": "../components/responses/errors.yaml#/NotFound"
          },
          "409": {
            "$ref": "../components/responses/errors.yaml#/Conflict"
          },
          "500": {
            "$ref": "../components/responses/errors.yaml#/ServerError"
          }
        },
        "parameters": [
          {
            "$ref": "../components/parameters/common.yaml#/IdPath"
          }
        ]
      }
    },
    "/api/v1/driver/bookings/{id}/reject": {
      "post": {
        "operationId": "driverBookingsPostByIdReject",
        "summary": "POST driver-bookings/:id/reject",
        "description": "Driver rejects a booking request. Requires a reason for rejection.",
        "tags": [
          "Driver Bookings"
        ],
        "security": [
          {
            "BearerAuth": []
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "../components/schemas/common.yaml#/ApiSuccessEnvelope"
                },
                "examples": {
                  "default": {
                    "$ref": "../components/examples/common.yaml#/DriverBookingSuccess"
                  }
                }
              }
            }
          },
          "400": {
            "$ref": "../components/responses/errors.yaml#/BadRequest"
          },
          "401": {
            "$ref": "../components/responses/errors.yaml#/Unauthorized"
          },
          "403": {
            "$ref": "../components/responses/errors.yaml#/Forbidden"
          },
          "404": {
            "$ref": "../components/responses/errors.yaml#/NotFound"
          },
          "409": {
            "$ref": "../components/responses/errors.yaml#/Conflict"
          },
          "500": {
            "$ref": "../components/responses/errors.yaml#/ServerError"
          }
        },
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "additionalProperties": false,
                "required": [
                  "reason"
                ],
                "properties": {
                  "reason": {
                    "type": "string",
                    "minLength": 1,
                    "maxLength": 500,
                    "description": "Reason for rejecting the booking (required, 1-500 characters)",
                    "example": "I have an emergency and cannot drive today"
                  }
                }
              }
            }
          }
        },
        "parameters": [
          {
            "$ref": "../components/parameters/common.yaml#/IdPath"
          }
        ]
      }
    },
    "/api/v1/driver/bookings/{id}/cancel": {
      "post": {
        "operationId": "driverBookingsPostByIdCancel",
        "summary": "POST driver-bookings/:id/cancel",
        "description": "Driver cancels a confirmed booking. Requires a reason for cancellation. Driver penalty may apply.",
        "tags": [
          "Driver Bookings"
        ],
        "security": [
          {
            "BearerAuth": []
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "../components/schemas/common.yaml#/ApiSuccessEnvelope"
                },
                "examples": {
                  "default": {
                    "$ref": "../components/examples/common.yaml#/DriverBookingSuccess"
                  }
                }
              }
            }
          },
          "400": {
            "$ref": "../components/responses/errors.yaml#/BadRequest"
          },
          "401": {
            "$ref": "../components/responses/errors.yaml#/Unauthorized"
          },
          "403": {
            "$ref": "../components/responses/errors.yaml#/Forbidden"
          },
          "404": {
            "$ref": "../components/responses/errors.yaml#/NotFound"
          },
          "409": {
            "$ref": "../components/responses/errors.yaml#/Conflict"
          },
          "500": {
            "$ref": "../components/responses/errors.yaml#/ServerError"
          }
        },
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "additionalProperties": false,
                "required": [
                  "reason"
                ],
                "properties": {
                  "reason": {
                    "type": "string",
                    "minLength": 1,
                    "maxLength": 500,
                    "description": "Reason for cancelling the booking (required, 1-500 characters)",
                    "example": "Vehicle broke down, cannot complete the trip"
                  }
                }
              }
            }
          }
        },
        "parameters": [
          {
            "$ref": "../components/parameters/common.yaml#/IdPath"
          }
        ]
      }
    },
    "/api/v1/driver/bookings/{id}/pickup-otp/verify": {
      "post": {
        "operationId": "driverBookingsPostByIdPickupOtpVerify",
        "summary": "POST driver-bookings/:id/pickup-otp/verify",
        "tags": [
          "Driver Bookings"
        ],
        "security": [
          {
            "BearerAuth": []
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "../components/schemas/common.yaml#/ApiSuccessEnvelope"
                },
                "examples": {
                  "default": {
                    "$ref": "../components/examples/common.yaml#/DriverBookingSuccess"
                  }
                }
              }
            }
          },
          "400": {
            "$ref": "../components/responses/errors.yaml#/BadRequest"
          },
          "401": {
            "$ref": "../components/responses/errors.yaml#/Unauthorized"
          },
          "403": {
            "$ref": "../components/responses/errors.yaml#/Forbidden"
          },
          "404": {
            "$ref": "../components/responses/errors.yaml#/NotFound"
          },
          "409": {
            "description": "A guard refused the verification, or it was forced while the ride was not in progress (FORCE_REQUIRES_RIDE_IN_PROGRESS).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "../components/schemas/common.yaml#/ApiErrorEnvelope"
                }
              }
            }
          },
          "500": {
            "$ref": "../components/responses/errors.yaml#/ServerError"
          }
        },
        "requestBody": {
          "required": false,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "otp": {
                    "type": "string",
                    "pattern": "^[0-9]{6}$",
                    "description": "Required unless force is true."
                  },
                  "force": {
                    "type": "boolean",
                    "default": false,
                    "description": "Push the verification through even though a guard refuses it."
                  },
                  "overrideReason": {
                    "type": "string",
                    "minLength": 5,
                    "maxLength": 500,
                    "description": "Why it had to be forced. Required whenever force is true."
                  }
                }
              }
            }
          }
        },
        "parameters": [
          {
            "$ref": "../components/parameters/common.yaml#/IdPath"
          }
        ],
        "description": "Verifies the pickup OTP. Sending force: true with a written overrideReason pushes it through a missing, expired, wrong or attempt-exhausted OTP and a booking in the wrong state. Forcing is only accepted while the ride is IN_PROGRESS (409 FORCE_REQUIRES_RIDE_IN_PROGRESS otherwise), writes a SUSPICIOUS ride event, warns the rider and opens a NEEDS_MANUAL_REVIEW dispute. otp may be omitted only when force is true."
      }
    },
    "/api/v1/driver/bookings/{id}/drop-otp/verify": {
      "post": {
        "operationId": "driverBookingsPostByIdDropOtpVerify",
        "summary": "POST driver-bookings/:id/drop-otp/verify",
        "tags": [
          "Driver Bookings"
        ],
        "security": [
          {
            "BearerAuth": []
          }
        ],
        "responses": {
          "200": {
            "description": "Success",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "../components/schemas/common.yaml#/ApiSuccessEnvelope"
                },
                "examples": {
                  "default": {
                    "$ref": "../components/examples/common.yaml#/DriverBookingSuccess"
                  }
                }
              }
            }
          },
          "400": {
            "$ref": "../components/responses/errors.yaml#/BadRequest"
          },
          "401": {
            "$ref": "../components/responses/errors.yaml#/Unauthorized"
          },
          "403": {
            "$ref": "../components/responses/errors.yaml#/Forbidden"
          },
          "404": {
            "$ref": "../components/responses/errors.yaml#/NotFound"
          },
          "409": {
            "description": "A guard refused the verification, or it was forced while the ride was not in progress (FORCE_REQUIRES_RIDE_IN_PROGRESS).",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "../components/schemas/common.yaml#/ApiErrorEnvelope"
                }
              }
            }
          },
          "500": {
            "$ref": "../components/responses/errors.yaml#/ServerError"
          }
        },
        "requestBody": {
          "required": false,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "otp": {
                    "type": "string",
                    "pattern": "^[0-9]{6}$",
                    "description": "Required unless force is true."
                  },
                  "force": {
                    "type": "boolean",
                    "default": false,
                    "description": "Push the verification through even though a guard refuses it."
                  },
                  "overrideReason": {
                    "type": "string",
                    "minLength": 5,
                    "maxLength": 500,
                    "description": "Why it had to be forced. Required whenever force is true."
                  }
                }
              }
            }
          }
        },
        "parameters": [
          {
            "$ref": "../components/parameters/common.yaml#/IdPath"
          }
        ],
        "description": "Verifies the drop OTP. Sending force: true with a written overrideReason pushes it through a missing, expired, wrong or attempt-exhausted OTP and a booking in the wrong state. Forcing is only accepted while the ride is IN_PROGRESS (409 FORCE_REQUIRES_RIDE_IN_PROGRESS otherwise), writes a SUSPICIOUS ride event, warns the rider and opens a NEEDS_MANUAL_REVIEW dispute. otp may be omitted only when force is true."
      }
    }
  }
}
